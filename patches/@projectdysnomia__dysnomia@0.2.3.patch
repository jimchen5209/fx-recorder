diff --git a/lib/voice/VoiceConnection.js b/lib/voice/VoiceConnection.js
index 66110cc89938dd8c380c922ff608b5c62fc142bf..1ff3aaa5c6bae4ed38032d7109b624f39a78c409 100644
--- a/lib/voice/VoiceConnection.js
+++ b/lib/voice/VoiceConnection.js
@@ -593,8 +593,15 @@ class VoiceConnection extends EventEmitter {
             this.emit("warn", `Voice WS close ${code}: ${reason}`);
             if(this.connecting || this.ready) {
                 let reconnecting = true;
-                if(code === 1000 || code === 4006 || code === 4021) {
+                if(code === 1000 || code === 4021) {
                     reconnecting = false;
+                } else if(code === 4006) {
+                    if (this.channelID) {
+                        reconnecting = true;
+                        err = null;
+                    } else {
+                        reconnecting = false;
+                    }
                 } else if(code === 4014 || code === 4022) {
                     if(this.channelID) {
                         data.endpoint = null;
@@ -984,10 +991,8 @@ class VoiceConnection extends EventEmitter {
             let dropPacket = false;
             if(this.daveSession && !data.equals(SILENCE_FRAME)) {
                 if(this.daveSession.ready && !userID) {
-                    this.emit("warn", "Packet was dropped due having an undefined user ID during a DAVE session");
                     dropPacket = true;
                 } else if(!this.daveSession.ready && this.daveProtocolVersion !== 0) {
-                    this.emit("warn", "Packet was dropped due to DAVE session not being ready yet");
                     dropPacket = true;
                 } else if(userID) {
                     try {
